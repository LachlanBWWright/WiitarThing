<Window x:Class="WiinUSoft.Windows.SyncWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sync Devices">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Style="{ThemeResource TitleTextBlockStyle}"
                   Text="Sync Wiimotes"
                   Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1"
                Spacing="6"
                Margin="0,0,0,12">
            <TextBlock Style="{ThemeResource BodyTextBlockStyle}"
                       Text="Press the red SYNC button on your controller to pair."/>
            <TextBlock Style="{ThemeResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap">
                <Run FontWeight="SemiBold"
                        Text="Close this window before playing"/>
                <Run Text=" — it causes input lag while scanning. You can copy the output below for troubleshooting."/>
            </TextBlock>
        </StackPanel>

        <Border Grid.Row="2"
                Margin="0,0,0,12"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12">
            <TextBox x:Name="promptBox"
                     ScrollViewer.VerticalScrollBarVisibility="Visible"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     FontFamily="Consolas"
                     FontSize="12"/>
        </Border>

        <Button Grid.Row="3"
                x:Name="cancelBtn"
                Content="Cancel Scan"
                Width="100"
                Height="36"
                HorizontalAlignment="Right"
                Click="cancelBtn_Click"/>
    </Grid>
</Window>
